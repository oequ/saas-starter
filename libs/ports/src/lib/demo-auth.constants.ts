/** Mock / demo login only — not for production. */
export const DEMO_AUTH_EMAIL = 'demo@example.com';

/** Pre-filled on the login form; accepted by `MockAuthAdapter` only. */
export const DEMO_AUTH_PASSWORD = 'OequDemo2026!';
