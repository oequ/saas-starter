# @oequ/ports-angular

Angular DI bindings for `@oequ/ports`.

Contains `InjectionToken` constants only. Port interfaces, models, and utils live in `@oequ/ports` (framework-agnostic).

```ts
import type { AuthPort } from '@oequ/ports';
import { AUTH_PORT } from '@oequ/ports-angular';
```
