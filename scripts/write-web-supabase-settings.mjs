#!/usr/bin/env node
/** Writes apps/web/src/app/supabase.settings.ts for local Supabase / CI. */
import { writeFileSync } from 'node:fs';
import { join } from 'node:path';

const url = process.env['SUPABASE_URL'] ?? 'http://127.0.0.1:54321';
const anonKey =
  process.env['SUPABASE_ANON_KEY'] ??
  'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6ImFub24iLCJleHAiOjE5ODM4MTI5OTZ9.CRXP1A7WOeoJeXxjNni43kdQwgnWNReilDMblYTn_I0';
const stripeEnabled =
  process.env['STRIPE_ENABLED'] === 'true' ||
  process.env['STRIPE_ENABLED'] === '1';

const target = join(
  process.cwd(),
  'apps/web/src/app/supabase.settings.ts',
);

const contents = `/** Generated by scripts/write-web-supabase-settings.mjs — do not commit secrets in prod. */
export const webSupabaseSettings = {
  url: '${url}',
  anonKey: '${anonKey}',
  stripeEnabled: ${stripeEnabled},
};
`;

writeFileSync(target, contents, 'utf8');
console.log(`Wrote ${target}`);
